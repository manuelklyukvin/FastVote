package klyuch.fastvote.build_src.configs

object GradleNamespaces {
    private const val BASE = "klyuch.fastvote"

    const val APP = "$BASE.app"
    const val CORE = "$BASE.core"

    const val VOTES = "$BASE.votes"

    const val HOME = "$BASE.home"
}